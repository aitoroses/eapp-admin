pagination = require('./paginationUtils');

describe 'Pagination when current is first', ->

describe 'Pagination when current is in middle', ->

describe 'Pagination when current is last', ->

  lastState = {isError: false, isLoading: false, perPage: 10, visible: 5, count: 51}

  it '' , ->
